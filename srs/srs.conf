listen              1935;
max_connections     1000;
daemon              off;
srs_log_tank        console;

http_api {
    enabled         on;
    listen          1985;
}

http_server {
    enabled         off;
}

vhost __defaultVhost__ {
    hls {
        enabled         on;
        hls_path        ./objs/nginx/html;
        hls_fragment    4;
        hls_window      30;
        hls_cleanup     on;
    }

    http_hooks {
        enabled         on;
        on_publish      http://api:3001/api/streaming/on-publish;
        on_unpublish    http://api:3001/api/streaming/on-unpublish;
    }
}
